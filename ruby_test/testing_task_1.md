### Testing task 1:

# Carry out static testing on the code below.
# Comment on any errors that you see below.
```ruby
### Testing task 2 code:

# Carry out dynamic testing on the code below.
# Correct the errors below that you spotted in task 1.

require_relative('card.rb')
class CardGame


  def checkforAce(card) #capital
    if card.value = 1
      return true
    else
      return false
    end
  end

  dif highest_card(card1 card2) #should be def, should be a comma between cards
  if card1.value > card2.value
    return card.name #no .name value
  else
    card2
  end
end #unnecessary end
end

def self.cards_total(cards) #no cards value
  total #no defined value to start
  for card in cards
    total += card.value
    return "You have a total of" + total #not interpolated and should return outside of the loop
  end
end


```
